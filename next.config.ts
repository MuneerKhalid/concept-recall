import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // output: 'export',
  images: {
    domains: ['cdn.conceptrecall.com'], // Add your allowed domains here
  },

};

export default nextConfig;
